# attic — superseded generations, kept for the record

Pre-MER-era tools replaced by the universal layer (2026-07-01+):
tune_antenna.py (one-command pipeline) replaced auto_tv/auto_tune/
antenna_profile; mer_gain_cal/mer_meter replaced gain ladders and
aim-by-power tools (find_signal, quality_finder, gaindist_sweep,
lock_test, chan_quality, eq_sweep); biast-aware probes replaced
lna_probe (which swallowed writeSetting errors). Nothing here is
imported by current code. History lives in git.
